[develop]
script_dir=$base/lib/dual_sharpa_wave
[install]
install_scripts=$base/lib/dual_sharpa_wave
[tool:pytest]
testpaths = test
junit_family = xunit2
markers =
    integration: runs installed ROS nodes in separate processes over DDS

